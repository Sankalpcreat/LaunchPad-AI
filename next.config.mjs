/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [ 'images.unsplash.com',"startup-template-sage.vercel.app"], // Add the domains you need
      },
};

export default nextConfig;
